{
  "name": "ai-test-automation-platform",
  "private": true,
  "scripts": {
    "test:newman": "newman run collections/demo.collection.json --env-var baseUrl=http://127.0.0.1:5001",
    "test:cypress": "cypress run"
  },
  "devDependencies": {
    "cypress": "^15.0.0",
    "newman": "^6.2.2"
  }
}
